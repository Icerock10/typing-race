{
    "extends": "../tsconfig.json",
    "include": ["**/*.ts", "**/*.js", "**/*.tsx"],
    "exclude": ["node_modules", "build", "frontend/public"],
    "compilerOptions": {
        "lib": ["DOM", "DOM.Iterable", "ESNext"],
        "jsx": "react-jsx",
        "baseUrl": ".",
        "outDir": "build",
        "paths": {
            "~/*": ["./src/*"]
        }
    }
}
